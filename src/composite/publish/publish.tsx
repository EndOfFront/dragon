import React, { Component } from "react";
import UIPublish from '../../components/publish/publish'
export default class Publish extends Component<any> {
  render() {
    return (
        <UIPublish />
    );
  }
}
